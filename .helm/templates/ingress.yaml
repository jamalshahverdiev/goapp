apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}-goapp
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.dns }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ .Chart.Name }}-goapp
            port:
              number: 8080